<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="App_theme" parent="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen">
        <item name="android:textColorHint">#4A148C</item>
        <item name="android:colorBackground">@color/light_blue</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:textAppearance">@style/MyText</item>
    </style>
    <!-- drawing when switching-->
    <!--<style name="AppTheme.TransNav" parent="App_theme">-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->
        <!--<item name="android:windowDrawsSystemBarBackgrounds">true</item>-->
    <!--</style>-->

    <style name="MyText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FFFFFF</item>
    </style>
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar">
        <!--<item name="android:background">@color/light_blue</item>-->
        <item name="android:background">#FFFFFF</item>
        <item name="android:textSize">@dimen/title_text_size</item>
    </style>
    <!-- Application theme. -->

</resources>
